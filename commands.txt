# Docker Startup
docker compose run --name ros2dev ros2 bash
docker exec -it ros2dev bash

docker ps -a
docker stop ros2dev
docker rm ros2dev

# Build
source /opt/ros/humble/setup.bash; colcon build --symlink-install; source install/setup.bash

# Run
ros2 run ros_behaviors_fsm finite_state_controller

#Gazeebo
ros2 launch neato2_gazebo empty_world.py
